# AppImage

AppImage artifacts are generated by the release workflow (see `.github/workflows/release.yml`).

Release output will include:

- `nexus_linux_amd64.AppImage`
- `nexus_linux_arm64.AppImage`
